This program does not require any input.

This program consists of a number of Players, who have different XP levels for several skills, called Badges, as well as an overall XP level.
The players earn XP by randomly performing a series of actions, each of which increases their XP level for that Badge, as well
as their overall XP level.

The simulation has constant variables that change how long it runs for, and how often is prints the status to the screen.
